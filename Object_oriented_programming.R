# there are 3 systems for OOP available in R
# S3 & S4 are old systems
# and RC (reference classes) is quite similar to modern OOP systems

# R classes allow to define attributes & methods
# and support inheritance from parent classes as well
# everything in R is an object
class(23)
class(12.12)
typeof(as.integer(12))
typeof(12)
class("Dummy")
class(class)
